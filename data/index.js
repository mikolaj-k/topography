import { AREA_JURA, AREA_PODHALE } from "../utils/constants";
import { AREA_META } from "./areas";
import { JURA_LOCATIONS } from "./jura/locations";
import {
  JURA_BOUNDS, JURA_ANCHORS, JURA_REGIONS, JURA_CATEGORIES,
  JURA_COORDINATES, JURA_OUTLINE, JURA_REGION_DIVIDERS,
} from "./jura/config";
import { PODHALE_LOCATIONS } from "./podhale/locations";
import {
  PODHALE_BOUNDS, PODHALE_ANCHORS, PODHALE_REGIONS, PODHALE_CATEGORIES,
  PODHALE_COORDINATES, PODHALE_OUTLINE, PODHALE_REGION_DIVIDERS,
} from "./podhale/config";

export const AREA_DATA = {
  [AREA_JURA]: {
    id: AREA_JURA,
    BOUNDS: JURA_BOUNDS,
    ANCHORS: JURA_ANCHORS,
    REGIONS: JURA_REGIONS,
    CATEGORIES: JURA_CATEGORIES,
    LOCATIONS: JURA_LOCATIONS,
    COORDINATES: JURA_COORDINATES,
    OUTLINE: JURA_OUTLINE,
    DIVIDERS: JURA_REGION_DIVIDERS,
    meta: AREA_META[AREA_JURA],
  },
  [AREA_PODHALE]: {
    id: AREA_PODHALE,
    BOUNDS: PODHALE_BOUNDS,
    ANCHORS: PODHALE_ANCHORS,
    REGIONS: PODHALE_REGIONS,
    CATEGORIES: PODHALE_CATEGORIES,
    LOCATIONS: PODHALE_LOCATIONS,
    COORDINATES: PODHALE_COORDINATES,
    OUTLINE: PODHALE_OUTLINE,
    DIVIDERS: PODHALE_REGION_DIVIDERS,
    meta: AREA_META[AREA_PODHALE],
  },
};

export { AREA_META };
